# Generated R2 stand-in (the serving layer; regenerated by CI / run_pipeline.sh)
serve/

# Generated fixture binaries (PDFs/zips). Produced deterministically by
# gen_fixtures.py (reportlab invariant + pinned deps), so CI reproduces identical
# content-addressed shas. Kept out of git per spec: binaries -> R2 only.
fixtures/**/files/

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS / editor
.DS_Store
*.swp
_search/node_modules/
